export class Doctor {
    constructor(login, name, rate, ratesCounter) {
        this.login = login;
        this.name = name;
        this.rate = rate;
        this.ratesCounter = ratesCounter;
    }
}